---
description: Save a fact, preference, or decision to CortexDB memory
---

Store the following into the CortexDB global memory by calling the `memory_save` MCP tool. Choose a stable, descriptive `memory_id` and concise `content`; use `scope=user` with `namespace=assistant` unless the user implies otherwise. If it is durable reference knowledge (not a personal preference), use `knowledge_save` instead. Confirm what you stored in one line.

Remember this:

$ARGUMENTS
